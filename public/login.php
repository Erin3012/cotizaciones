<?php
declare(strict_types=1);
require_once dirname(__DIR__).'/app/bootstrap.php';
if(current_user()) redirect('index.php?page=quotes');
$error=null;
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$email=trim((string)($_POST['email']??''));$password=(string)($_POST['password']??'');$s=db()->prepare('SELECT * FROM users WHERE email=? AND active=1');$s->execute([$email]);$user=$s->fetch();if($user&&password_verify($password,$user['password_hash'])){login_user($user);redirect('index.php?page=quotes');}$error='Correo o contraseña incorrectos.';}
?><!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Acceso · Cotizaciones Metalrubber</title><link rel="stylesheet" href="assets/style.css"></head><body><main class="login-wrap"><form class="login-card" method="post"><div class="brand" style="color:#111214;padding:0 0 24px"><span class="brand-mark">M</span><span><strong>METALRUBBER</strong><small style="color:#687075">Cotizaciones</small></span></div><h1>Acceso<br><em style="color:#b09d00;font-style:normal">administrativo</em></h1><p class="hint">Ingresa para crear y consultar cotizaciones.</p><?php if($error): ?><div class="alert error"><?=e($error)?></div><?php endif; ?><?=csrf_field()?><div class="field"><label for="email">Correo</label><input class="input" id="email" name="email" type="email" required autofocus></div><div class="field" style="margin-top:15px"><label for="password">Contraseña</label><input class="input" id="password" name="password" type="password" required></div><div class="actions"><button class="btn btn-primary" type="submit">Ingresar</button></div></form></main></body></html>
